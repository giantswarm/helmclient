apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-tests
  namespace: default
data:
  run.sh: |-
    @test "Testing nginx is reachable" {
      response=$(curl -I nginx.default 2> /dev/null | head -n 1 | cut -d$' ' -f2)
      [ "$response" -eq 200 ]
    }
