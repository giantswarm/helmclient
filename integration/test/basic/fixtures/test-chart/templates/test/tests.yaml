apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.name }}-tests
data:
  run.sh: |-
    @test "Testing nginx is reachable" {
      response=$(curl -I {{ .Values.pod.name }}.{{ .Values.pod.namespace }} 2> /dev/null | head -n 1 | cut -d$' ' -f2)
      [ "$response" -eq 200 ]
    }
