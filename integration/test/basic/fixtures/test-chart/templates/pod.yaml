apiVersion: v1
kind: Pod
metadata:
  name: {{ .Values.pod.name }}
  namespace: {{ .Values.namespace }}
  labels:
    name: {{ .Values.pod.name }}
spec:
  containers:
  - name: {{ .Values.pod.name }}
    image: {{ .Values.pod.image.repository }}
    ports:
    - containerPort: 80
